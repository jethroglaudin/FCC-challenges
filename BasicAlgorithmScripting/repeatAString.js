function repeatStringNumTimes(str, num) {
    // repeat after me
    let strArr = str.split()
    console.log(strArr)

    
    for (let i = 0; i < num; i++){
        arr.push(str)
    }
    arr = arr.join("");
    console.log(arr);
    return str;

  
  }
  
  repeatStringNumTimes("abc", 3);